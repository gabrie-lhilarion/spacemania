export default {
    darkMode: "class", // Ensure dark mode works via a class
    content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"], // Adjust paths
    theme: {
        extend: {},
    },
    plugins: [],
};
